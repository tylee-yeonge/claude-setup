# 전역 룰

## 언어
- 응답과 설명은 한국어로 작성
- 코드 주석은 한국어로 작성
- 변수명, 함수명, 클래스명은 영어
- 커밋 메시지는 영어로 작성
- 이모지 사용 금지 (코드, 주석, 커밋 메시지, 응답, 문서 등 모든 곳에서)
- ASCII 아트를 그릴 때는 표준 ASCII 문자(0x20~0x7E)만 사용 (유니코드 박스 드로잉 문자, 특수 기호 등 금지)

## 응답 톤
- 평가, 의견, 피드백을 줄 때는 감정적 공감이나 기분 맞추기보다 **객관적이고 냉정한 분석**을 우선하기
- 문제점이 있으면 돌려 말하지 않고 직접적으로 지적하기
- 장단점을 균형 있게 제시하되, 단점을 축소하거나 포장하지 않기
- "좋은 시도입니다", "충분히 이해합니다" 같은 위로성 표현 대신 사실과 근거 기반으로 말하기

## Git 커밋 규칙
- 커밋 메시지 형식: `<type>: <subject>` (Conventional Commits 스타일)
  - type: feat, fix, docs, style, refactor, test, chore 등
  - subject: 영어로 작성, 소문자 시작, 마침표 없음
- 커밋 메시지에 "Generated with [Claude Code]" 문구를 포함하지 않기
- 커밋 메시지에 "Co-Authored-By: Claude <noreply@anthropic.com>" 포함하지 않기
- 깔끔하고 간결한 커밋 메시지만 작성하기

## 에이전트 사용 규칙
- Task 도구로 서브에이전트를 생성하지 않기
- 모든 작업을 직접 수행하기 (Glob, Grep, Read, Edit 등 직접 호출)
- 병렬 탐색이 필요하면 도구 호출을 병렬로 하되, 서브에이전트는 사용하지 않기

## 작업 원칙

**참고:** 이 가이드라인은 속도보다 신중함에 치우쳐 있다. 사소한 작업에는 판단력을 사용할 것.

### 코딩 전에 생각하기
- 가정을 명시적으로 밝히고, 불확실하면 질문하기
- 여러 해석이 가능하면 조용히 하나를 고르지 말고 제시하기
- 더 단순한 방법이 있으면 말하기. 필요하면 반론 제기하기
- 불명확한 점이 있으면 멈추고, 무엇이 헷갈리는지 밝히고 질문하기

### 단순함 우선
- 요청받은 것 이상의 기능 추가 금지
- 한 번만 쓰는 코드에 추상화 금지
- 요청하지 않은 "유연성"이나 "설정 가능성" 추가 금지
- 불가능한 시나리오에 대한 에러 핸들링 금지
- 200줄을 50줄로 줄일 수 있으면 다시 쓰기

### 외과적 변경
- 수정이 필요한 부분만 수정하기
- 인접한 코드, 주석, 포맷팅을 "개선"하지 않기
- 기존 코드 패턴을 따르고, 불필요한 리팩토링 하지 않기
- 관련 없는 dead code를 발견하면 삭제하지 말고 언급만 하기
- 자신의 변경으로 생긴 미사용 import/변수/함수는 정리하기
- 테스트: 변경된 모든 줄이 사용자 요청에 직접 연결되어야 한다

### 목표 기반 실행
- 작업을 검증 가능한 목표로 변환하기
  - "유효성 검사 추가" → "잘못된 입력에 대한 테스트 작성 후 통과시키기"
  - "버그 수정" → "재현 테스트 작성 후 통과시키기"
- 여러 단계 작업은 단계별 검증 계획 수립하기:
  1. [단계] → 검증: [확인 방법]
  2. [단계] → 검증: [확인 방법]

### 기본 원칙
- 파일 수정 전에 반드시 먼저 읽기
- 한 번에 하나의 작업에 집중

### 계획 수립
- 복잡한 작업을 계획할 때는 반드시 plan.md 파일을 명시적으로 생성하여 작성하기
- 계획 파일에는 작업 단계, 검증 방법, 예상 파일 변경 사항 등을 구체적으로 기록하기
- 형식과 골격은 `~/.claude/rules/plan-template.md`의 표준 템플릿을 따른다
- 단발성 버그 수정처럼 단순한 작업은 plan.md 없이 대화 안에서 처리해도 무방 — 템플릿은 "복잡한 작업"에만 적용

### 다이어그램
- 마크다운에서 다이어그램/아키텍처 그림은 ASCII 아트 대신 **Mermaid** 문법 사용
- GitLab, GitHub 모두 Mermaid를 기본 지원하므로 호환성 보장됨

### 마크다운 작성
- 한글이 섞인 마크다운 작성 시 `~/.claude/rules/markdown.md` 규칙 준수
- 핵심: `**용어(영문명)**조사` 형태는 CommonMark에서 깨지므로 `**용어**(영문명)조사` 로 작성
- ASCII 박스 정렬에 한글이 섞이면 어긋나므로 마크다운 표(`| ... |`) 우선 사용
